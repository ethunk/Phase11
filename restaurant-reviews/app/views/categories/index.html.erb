<h1>
  Categories
</h1>


<% @categories.each do |category| %>
  <div class="card" style="width: 300px;">
    <div class="card-divider">
      <%= link_to("#{category.name}", category_path(category.id), :id => "category_name") %>
    </div>
  </div>
<% end %>
